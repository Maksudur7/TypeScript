const course = "Next level web devlopment"

console.log(course);

let userName: string = 'Maksudur'
console.log(userName);

let bazarList: string[] = ['eggs', 'milk']

// reference type : object

const user: { readonly fristName: string;  middleName?: string; lastName: string } = {
    fristName: 'Mexbaul',
    middleName: 'abedin',
    lastName: 'Forhan'
}